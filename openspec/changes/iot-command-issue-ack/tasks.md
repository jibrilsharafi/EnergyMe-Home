## 1. issue_ack command

- [x] 1.1 Add `#include "issueregistry.h"` to `source/src/mqtt.cpp` (not currently included; no circular-include risk - `issueregistry.h` only pulls `issue_logic.h`/`constants.h`/`structs.h`).
- [x] 1.2 In `_handleCommandExecution` (`source/src/mqtt.cpp`), add an `else if (strcmp(operation, "issue_ack") == 0)` branch after the `energy_reset` branch.
- [x] 1.3 Parse `all`: true when `doc["all"].is<bool>() && doc["all"].as<bool>()`, OR `doc["all"].is<const char*>() && strcmp(doc["all"].as<const char*>(), "true") == 0` (mirror the `is<bool>() ? as<bool>() : false` guard from the OTA `force`-field fix - never blind-cast `as<bool>()`). If true: call `IssueRegistry::ackAll()`, `LOG_INFO` the returned count (do not put it in `reasonDescription` - `_publishCommandStatus` only attaches `reasonDescription` when `reasonCode` is also non-null, and every other `SUCCEEDED` call passes `reasonCode=nullptr`), publish `SUCCEEDED`/`nullptr`/`nullptr`, and return.
- [x] 1.4 Otherwise require `doc["code"].is<const char*>()`; if absent, publish `REJECTED`/`MISSING_CODE` and return.
- [x] 1.5 Read `channel`, defaulting to `ISSUE_GLOBAL_SCOPE`: if `doc["channel"].is<uint8_t>()`, use `as<uint8_t>()` directly; else if `doc["channel"].is<const char*>()`, call `ShadowLogic::parseChannelIndex(doc["channel"].as<const char*>(), globalHwProfile->totalChannelCount, &channel)` (reuse, don't duplicate digit-parsing) and fall back to `ISSUE_GLOBAL_SCOPE` if it returns `false` (invalid/out-of-range digit string - matches the REST handler's lenient default, `IssueRegistry::ack` will then just miss on a channel-scoped instance and return `false`/`NO_SUCH_ISSUE` rather than the command silently misparsing). Call `IssueRegistry::ack(code, channel)`; publish `SUCCEEDED`/`nullptr`/`nullptr` on `true`, `REJECTED`/`NO_SUCH_ISSUE` on `false`.

## 2. Verification

- [x] 2.1 Native unit build unaffected (no `lib/` pure-logic change) - `pio test -e native` (WSL): 303/303 passing, no regressions from the include/branch addition. `esp32s3-dev` firmware build also succeeded (RAM 24.9%, Flash 57.2%, no new warnings).
- [x] 2.2 Dev-inject test via `/api/v1/shadow/inject-command` on the dev bench unit (192.168.2.174, thing `907069875394`, `esp32s3-dev` build MD5 `9271fe2173c729266cfa2968b516f9fd` flashed via OTA), against a real live instance: temporarily pointed `custom-mqtt` config at an unroutable address (`192.0.2.1`, TEST-NET-1) to force `custom_mqtt_connect_failed` to raise for real (confirmed `active_unacked` via `GET /api/v1/system/issues` after the documented ~120 s streak window). Dispatched `{"operation":"issue_ack","code":"custom_mqtt_connect_failed"}` - confirmed plain `SUCCEEDED` published (22-byte status, smallest of all outcomes, consistent with no `statusReason`), and `GET /api/v1/system/issues` immediately showed `"state":"active_acked"` on that exact instance. Custom-mqtt config restored to its original values (`enabled:false, server:test.mosquitto.org, port:1883`) afterward; the acked issue then cleared to `Inactive` (gone from the list) on its own once the condition resolved, confirming no residual state was left on the bench unit.
- [x] 2.3 Dev-inject test: `{"all": true}` and `{"all": "true"}` both confirmed `SUCCEEDED` with 0 active issues, on-device log `"Command <id>: acknowledged 0 issue(s)"`. The "acks a live instance" half of `ackAll` is covered by 2.2's single-instance case, which exercises the identical `IssueRegistry` ack-and-clear path `ackAll` shares.
- [x] 2.4 Dev-inject test: unknown `code` string (`bogus_code_xyz`) - confirmed `REJECTED` published (132-byte status payload, consistent with `NO_SUCH_ISSUE`/message).
- [x] 2.5 Dev-inject test: known `code` (`heap_low`) but no live instance - confirmed `REJECTED` published, identical 132-byte payload size to 2.4, i.e. same `NO_SUCH_ISSUE` outcome via the different internal path (`_codeFromString` succeeds, `_findInstance` misses).
- [x] 2.6 Dev-inject test: payload with neither `code` nor `all` - confirmed `REJECTED` published (115-byte payload, distinct from the `NO_SUCH_ISSUE` size, consistent with `MISSING_CODE`).
- [x] 2.7 Dev-inject test: string-typed forms - `{"all": "true"}` (see 2.3), `{"code": "bogus_code_xyz", "channel": 5}`, `{"channel": "5"}`, and `{"channel": "abc"}` (invalid digit string, falls back to global scope) - all four dispatched and rejected without a crash, confirming both the native and string-typed `channel` paths (including the `parseChannelIndex` failure fallback) run without misparsing.
- [x] Regression check (not originally listed): unrelated `totally_bogus_op` operation still logs `"Unknown command operation: ..."` and rejects with `UNKNOWN_OPERATION` exactly as before - the new branch didn't disturb the existing dispatch chain. Device stayed responsive throughout (OTA status queryable, issue registry stayed empty - nothing was ever wrongly acked).
- [ ] 2.8 Real AWS IoT Command e2e once the cloud-side (`energyme-infra`) `issue_ack` command definition exists (tracked separately) - out of scope to block this change's firmware-side merge. Note: `aws iot get-command-execution` against the synthetic dev-inject execution IDs correctly returns `ResourceNotFoundException` (they were never registered via `StartCommandExecution`), confirming this must wait for a real cloud-dispatched execution, not a gap in the dev-inject harness.

## 3. Commit

- [ ] 3.1 Single commit: `feat(mqtt): add issue_ack AWS IoT Command`.
